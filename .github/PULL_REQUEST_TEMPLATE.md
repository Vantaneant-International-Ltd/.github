**Repo / bucket:** <repo name> (infrastructure | house | client | personal)

**What this changes**

**Why**

### Checks
- [ ] Follows org-standards: naming, required files, branch model
- [ ] No secrets, keys, tokens, or `.env` values added (public anon keys excepted)
- [ ] Lockfile updated if dependencies changed

### Destructive change
- [ ] This PR deletes or renames a repo, flips visibility, drops a branch, or
      changes live data. If checked, it needs explicit owner approval before merge
      and must be described above. Default is no.
